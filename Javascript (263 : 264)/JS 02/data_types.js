// Strings - letters or characters or sentences or text
let countryName = "Bangladesh"; // string
let capital = "Dhaka";
let sentence = `${capital} is the capital of ${countryName}`; // template literal
console.log(sentence);

console.log(typeof countryName); // to findout the type of a variable

// Numbers
let age = 20;
let height = 5.5;
console.log(age);
console.log(typeof age);

// Boolean
let isHot = true;
const isCold = false;

console.log(typeof isHot); // boolean

//Undefined
let car;
console.log(car); // it will show undefined since there is no value assign to it
console.log(typeof car); // type will show undefined as well

//Null
let house = null; // null means empty / no value
console.log(typeof house); // object

//NaN - not a number
let counter = 35;
console.log(45 * "word");

